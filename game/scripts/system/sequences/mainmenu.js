import Sequence from "../classes/sequence.js";

const MainMenu = new Sequence( );

MainMenu.onEnter = function( options ) {
    
}

MainMenu.onExit = function( options ) {
    
}

export default MainMenu;